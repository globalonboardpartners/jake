DEV_FRONTEND_URL=http://localhost:3000
FRONTEND_URL=https://group.exploro.com
DATABASE_URL=postgres://root:root@[IP Address]:5440/exploro


replace [IP Address] with the appropriate host here.
You can find it with this command on Mac + Ubuntu:
`ifconfig | grep inet`
Several ipaddresses will pop up. Try each of them until
you get one to work. For ubuntu, it's usually the first
one on the third line of stdout.
